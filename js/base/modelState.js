export {ModelState};

import { Enum }             from "./enum.js";

/** ========================================================================
 * ModelState enum
 */
class ModelState extends Enum {}